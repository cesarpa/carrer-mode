services:
  - type: web
    name: carrer-mode
    runtime: docker
    plan: free
    region: ohio
    envVars:
      - key: PORT
        value: 8080
      - key: SPRING_PROFILES_ACTIVE
        value: prod
